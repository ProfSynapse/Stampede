{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "./run.sh run --profile coursecast --pattern school-bell --max-vus 10000 --auth-strategy hmac",
    "restartPolicyType": "NEVER",
    "numReplicas": 1,
    "healthcheckPath": null
  }
}
